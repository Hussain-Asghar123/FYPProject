<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#E31212">

    <!-- Close + Title Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="14dp">

        <TextView
            android:id="@+id/tvClose"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✕"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:paddingEnd="12dp"
            android:clickable="true"
            android:focusable="true"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Substitute"
            android:textColor="#FFFFFF"
            android:textSize="13sp"/>
    </LinearLayout>

    <!-- Playing Spinner -->
    <Spinner
        android:id="@+id/spinnerPlaying"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_spinner_white_outline"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:popupBackground="@android:color/white"/>

    <View android:layout_width="match_parent" android:layout_height="10dp"/>

    <!-- Benched Spinner -->
    <Spinner
        android:id="@+id/spinnerBenched"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_spinner_white_outline"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:popupBackground="@android:color/white"/>

    <!-- Substitute Button -->
    <Button
        android:id="@+id/btnSubstitute"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="14dp"
        android:text="Substitute"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:paddingHorizontal="28dp"
        android:background="@drawable/bg_btn_white_outline"/>

</LinearLayout>